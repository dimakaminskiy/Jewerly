@model IList<Jewerly.Domain.SliderPicture>

@if (Model.Any())
{
      <!---Слайдер---->
    <div id="myCarousel" class="carousel slide hidden-xs shadow" data-ride="carousel">
        <!-- Indicators -->
        <ol class="carousel-indicators">
            <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
             @for (int i = 1; i < Model.Count(); i++)
             {
                <li data-target="#myCarousel" data-slide-to="@i"></li>
             }
        </ol>

        <!-- Wrapper for slides -->
        <div class="carousel-inner" role="listbox">

            @{
                var f = Model.First();
            }

            <div class="item active">
                <img src="@f.Path" alt="@f.Caption" class="img-responsive">
                <div class="carousel-caption">
                    <h3>@f.Caption</h3>
                    <p>@f.Text</p>
                </div>
            </div>


            @for (int i = 1; i < Model.Count(); i++)
            {
             var item = Model[i];
                <div class="item">
                    <img src="@item.Path" alt="@item.Caption" class="img-responsive">
                    <div class="carousel-caption">
                        <h3>@item.Caption</h3>
                        <p>@item.Text</p>
                    </div>
                </div>
            }

        </div>

        <!-- Left and right controls -->
        <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
            <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
            <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>
            <!---СлайдерEnd---->
}
else
{

}

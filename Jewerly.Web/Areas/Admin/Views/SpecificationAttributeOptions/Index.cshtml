@model IEnumerable<Jewerly.Domain.SpecificationAttributeOption>

@{
    ViewBag.Title = "Опции специальных атрибутов";
    Layout = "~/Areas/Admin/Views/Shared/AdminLayout.cshtml";
}

<h2>Опции специальных атрибутов</h2>

<p>
    @Html.ActionLink("Создать", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProductSpecificationAttribute.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DisplayOrder)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProductSpecificationAttribute.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DisplayOrder)
        </td>
        <td>
            <a href="@Url.Action("Edit", new {id = item.SpecificationAttributeOptionId})"><span class="glyphicon glyphicon-pencil"></span></a>
            <a href="@Url.Action("Delete", new {id = item.SpecificationAttributeOptionId})"><span class="glyphicon glyphicon-trash"></span></a>
        </td>
    </tr>
}

</table>
